# Kopiere diese Datei nach dev/.env und passe die Werte an. .env wird NICHT committet.

# Datenbank
DB_ROOT_PASSWORD=rootsecret
DB_NAME=humhub
DB_USER=humhub
DB_PASSWORD=humhub

# Stripe (Test-Modus). Leer lassen, solange noch nicht eingerichtet.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
